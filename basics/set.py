s1=set()
s1=set("savitri") # when we use set just have pass only one argument
s2={2,5,'ywui'}  # otherwise another way to use set

s3={}     # this becomes empty dictionary when put some values then becomes set
print(s1)
print(s2)